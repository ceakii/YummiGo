import { Box } from "@mui/material";
import { pageStyle } from "../../Style";

export default function ChickenPorridge() {
  return (
    <Box sx={pageStyle}>
      ChickenPorridge page
    </Box>
  );
}